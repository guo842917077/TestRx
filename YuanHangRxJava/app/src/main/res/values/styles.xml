<resources>

    <style name="OverflowToolbar" parent="@android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@drawable/ic_action_menu</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- toolbar（actionbar）颜色 -->
        <item name="colorPrimary">@color/LightBlue</item>
        <!-- 状态栏颜色-->
        <item name="colorPrimaryDark">@color/black</item>
        <!-- 选中颜色 -->
        <item name="colorAccent">#F0F0F0</item>
        <!-- 窗口的背景颜色 -->
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:actionOverflowButtonStyle">@style/OverflowToolbar</item>
    </style>

    <style name="line_gray">
        <item name="android:background">@color/line_gray</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1px</item>
    </style>
    <!-- 标题1样式 -->
    <style name="TxtTitleBaseStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/black</item>
    </style>
</resources>
